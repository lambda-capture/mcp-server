{
  "name": "lambda-capture-mcp",
  "version": "1.0.0",
  "description": "Model Context Protocol server for Lambda Capture semantic search",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "lambda-capture-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "lambda-capture",
    "semantic-search"
  ],
  "author": "Lambda Capture Limited",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "axios": "^1.6.0",
    "zod": "^3.22.0",
    "zod-to-json-schema": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lambda-capture/mcp-server.git"
  },
  "bugs": {
    "url": "https://github.com/lambda-capture/mcp-server/issues"
  },
  "homepage": "https://github.com/lambda-capture/mcp-server#readme"
}
